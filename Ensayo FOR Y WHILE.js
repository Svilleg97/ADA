var suma = 1;
while(suma <= 5) {
    console.log(suma)
    suma ++;
    };
// EJEMPLO DE UN CONTADOR CON BUCLE WHILE

// EJEMPLO DE UNA VARIABLE QUE SE INCREMENTA EN UNO
var incremento = 3; 
while(incremento <= 3) {
    incremento = incremento + 1
    console.log(incremento);
    };
    // En este caso no se genera un contador, pues la variable se mantiene estatica
    // dentro del bucle, estamos generando una condición que es que incremento sea menor o igual a 3
    // para que el bucle pueda seguir ejecutandose, de lo contrario no se cumple la condición.
    // posteriormente le estamos incrementando +1 a la variable, y por eso imprime en la consola 4. 
   
